package io.github.devthedevil.mailstore.model;

/** Well-known mailbox folders. Stored as text so custom folders can be added later. */
public enum Folder {
    INBOX,
    SENT,
    DRAFTS,
    TRASH,
    JUNK
}
